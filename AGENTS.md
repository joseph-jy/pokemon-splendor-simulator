# 포켓몬 스플렌더 시뮬레이터

## 개요
 포켓몬 스플렌더를 웹 브라우저에서 구동하는 시뮬레이터. 사용자 1명 + AI 3명이 한 판을 진행하며, 진행 중 각 플레이어의 실시간 승리 확률을 표시한다.
 - 게임 규칙·카드 목록·용어: [GAME.md](GAME.md)
 - AI 전략: [STRATEGY.md](STRATEGY.md) (에이전트용 의사결정 정책 — 팩트·평가함수·행동규칙 3층 구조)

## 핵심 설계 결정
 "실시간 승리 확률"이 본 시뮬레이터의 차별점이다. 이 계산 방식은 다른 문서에 없으므로 여기에만 명시한다. (게임 규칙 자체는 GAME.md 참조)
 - **산출 방식**: 몬테카를로 시뮬레이션. 현재 상태에서 종료 조건까지 가상 플레이아웃을 N회 돌려, 각 플레이어가 1위가 된 비율 = 승리 확률.
 - **플레이아웃 정책**: AI 3명은 STRATEGY.md 전략. 사용자의 미래 행동은 기본 정책(아래 결정)으로 가정.
 - **갱신**: 매 행동 종료 직후 재계산, UI는 비동기로 갱신(본 게임 진행을 막지 않음). N 기본 200, 응답시간에 따라 적응 조정. 계산은 **Web Worker**에서 수행해 메인 스레드를 막지 않음(브라우저 UI 응답성 보장).

> **결정됨 (b)**: 사용자 미래 행동의 기본 정책 = 탐욕적 휴리스틱. STRATEGY.md §2·§3의 경량 버전(V_card 상위 후보 + 약간 노이즈)으로 플레이아웃. 이유: 구현 비용 대비 플레이아웃 품질 우수. AI 정책과 사용자 정책은 `simulator`가 별도 호출.

## 완료 기준
 다음이 모두 충족될 때 프로젝트 완료:
 1. 2~4인(사용자 1 + AI 1~3)이 규칙에 맞게 시작 → 종료까지 진행. 인원은 시작 화면에서 선택.
 2. 매 행동 후 각 플레이어 승리 확률이 갱신되어 표시됨.
 3. 사용자가 자기 차례에 합법 행동(볼 획득 / 카드 보관 / 카드 획득 / 진화)을 선택 가능.
 4. 불가능 행동은 차단되고 사유가 안내됨.
 5. 종료 시 승리 조건(총점 → 진화 카드 수 → 획득 카드 수)으로 최종 결과 표시.
 6. 종료 후 새 게임(재시작) 가능 — 새 시드로 초기화하여 처음부터 다시 진행.

## 제약
 - 규칙 위반 행동(볼 10개 초과 보유, 보관 3장 초과, 희귀/전설/환상 카드 보관 등)은 엔진 단에서 거부.
 - 같은 시드 → 같은 결과. 난수 생성기를 시드 제어 가능하게 둔다(재현/디버깅용).
 - 복잡한 2D/3D·게임 엔진 라이브러리(Phaser 등) 미사용. 보드/카드/토큰은 DOM/CSS로 표현.
 - 언어: TypeScript. 게임 상태는 엔진이 단일 소스로 보유.
 - **빌드 산출물 = 단일 HTML**: JS·CSS·이미지 에셋까지 모두 인라인(Vite 등 번들러의 에셋 인라인 옵션). 정적 호스팅 1파일 배포로 서버 없이 누구나 즉시 플레이.
 - UI는 **Vanilla TS 권장**(단일 HTML 크기 최소화; React 런타임 정적 분량 회피). React도 기술적으로 가능.

## 모듈 분리 (제약)
 `game`(순수 규칙 엔진) · `strategy`(AI 정책) · `simulator`(몬테카를로, game+strategy에만 의존) · `ui`(렌더링·입력) · `data`(정적 카드/볼 데이터) · `tuning`(자기대국 아레나, game+strategy에만 의존 — 브라우저 번들에 포함되지 않음). `simulator`·`tuning`은 `ui`를 의존하지 않아야 한다.

## 이미지 리소스
 이미지는 `assets/<단계>/<romanized>.png` 경로 규칙으로 저장. 파일명이 곧 식별자이며 `data` 모듈이 직접 참조. 상세 규칙·매니페스트·romanization 매핑은 [assets/ASSETS.md](assets/ASSETS.md) 참조. 디렉토리: `balls/` · `rare/` · `stage1/` · `stage2/` · `stage3/` · `ui/`.

## 참고 문서
 - [GAME.md](GAME.md) — 게임 규칙·카드 목록·용어.
- [STRATEGY.md](STRATEGY.md) — AI 전략 정의 + 사용자 기본 정책(플레이아웃용).
 - [AI_PLAN.md](AI_PLAN.md) — AI 강화 로드맵(자기대국 튜닝 → MCTS → 신경망) + 치트 모드 계획.
 - [assets/ASSETS.md](assets/ASSETS.md) — 이미지 리소스 규칙·매니페스트.
