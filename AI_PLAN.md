# AI 강화 로드맵 (AI_PLAN.md)

AI 실력을 단계적으로 끌어올리고, 그 결과를 치트 모드(승률·최선 수 표시)에 연결하기 위한 계획.
각 단계는 독립적으로 완결되며, **한 단계를 완료·체감한 뒤 다음 단계로 진행**한다.

## 배경

- 현재 AI: 손으로 튜닝한 휴리스틱. `src/strategy/weights.ts`의 가중치 +
  `src/strategy/policy.ts`의 평가함수(`cardValue`/`actionValue`/`stateEval`)와
  얕은 탐색(`chooseStrongTurn`: 상위 후보 18개 × greedy rollout 1회).
- 엔진(`src/game/`)은 UI 무의존·복제 가능(`cloneGame`)·시드 RNG라서
  Node 헤드리스 자기대국(초당 수십~수천 판)이 바로 가능하다.
- 몬테카를로 승률(`src/simulator/montecarlo.ts`) + Web Worker가 이미 있어
  치트 모드의 기반이 갖춰져 있다.

핵심 원리: **치트 모드의 승률/추천 정확도는 시뮬레이션에 쓰는 정책의 실력에 비례한다.**
AI를 강화하는 각 단계가 곧 치트 모드의 품질 향상이다.

---

## 1단계 — 자기대국 가중치 튜닝 (진행 중)

사람이 정한 가중치·상수를 자기대국 승률로 자동 최적화한다. 구조 변경 없이
"같은 알고리즘, 더 좋은 파라미터"로 실력을 올리는 단계.

### 방법

1. **파라미터화**: `WEIGHTS`(9개) + `policy.ts`에 흩어진 평가·탐색 상수 중
   영향이 큰 것들을 `StrategyWeights` 하나로 모아 모든 정책 함수에 주입 가능하게 한다.
   기본값은 기존 값 그대로 → 동작 불변.
2. **아레나**(`src/tuning/arena.ts`): 플레이어마다 다른 가중치를 줄 수 있는
   헤드리스 자기대국 러너. 좌석 로테이션 + 시드 고정(공통 난수)으로 분산을 줄인다.
3. **CEM 튜닝**(`scripts/tune.ts`): Cross-Entropy Method.
   - 파라미터별 (평균, 표준편차)에서 후보 개체군 샘플링
   - 각 후보를 기준(baseline) 가중치와 N판 대결시켜 적합도 = 승률 측정
   - 상위(엘리트) 후보들로 평균·표준편차 갱신, 반복
   - 실행: `npx vite-node scripts/tune.ts`
4. **검증·반영**: 최종 후보를 기준 가중치와 대규모 검증 매치(수백 판, 튜닝과 다른 시드)로
   확인 후, 유의하게 강하면 `weights.ts` 기본값을 교체한다.

### 성공 기준

- 튜닝된 가중치가 기존 기본값 상대로 검증 매치에서 **유의하게 높은 승률**
  (4인전 1 vs 기준 3에서 기준선 25%를 신뢰구간 밖으로 상회).

### 산출물

- `src/strategy/weights.ts` — `StrategyWeights` 타입 + 기본값
- `src/tuning/arena.ts` — 자기대국 러너
- `scripts/tune.ts` — CEM 튜닝 스크립트(결과를 JSON으로 기록)

---

## 2단계 — MCTS 탐색 (예정)

평가함수는 그대로 두고 탐색을 UCT 기반 MCTS로 교체해 수읽기 깊이를 늘린다.
파라미터 튜닝보다 큰 폭의 실력 향상이 기대되는 단계.

### 방법

- `chooseStrongTurn`을 대체하는 `chooseMctsTurn`:
  선택(UCT) → 확장 → 시뮬레이션(1단계 튜닝된 greedy 정책) → 역전파.
- **비공개 정보 처리(determinization)**: 덱 순서·상대의 비공개 보관 카드는 관측 불가이므로,
  시뮬레이션마다 미공개 카드를 다시 섞어 여러 "가능 세계"를 평균한다(ISMCTS 방식).
  현재 `cloneGame`은 덱 순서를 그대로 복제하므로 셔플 유틸 추가 필요.
- 시간 예산 기반(턴당 예: 300ms~1s) 반복. Web Worker에서 실행해 UI 비차단.
- 난이도 옵션: 반복 횟수로 쉬움/보통/어려움 조절 가능(부산물).

### 성공 기준

- MCTS AI가 1단계 튜닝 AI 상대 검증 매치에서 유의하게 높은 승률.
- 사람 체감: 브라우저에서 턴당 지연 1초 이내.

### 치트 모드 연계

- MCTS 루트의 자식 방문 횟수·평균 가치가 곧 **행동별 승률 랭킹** →
  치트 모드 추천을 MCTS 결과로 교체하면 정확도가 한 단계 상승.

---

## 3단계 — 신경망 자기대국 (AlphaZero-lite, 선택)

수작업 평가함수를 학습된 policy+value 네트워크로 대체한다.
가장 강하지만 비용이 커서, 2단계 결과가 아쉬울 때만 진행한다.

### 방법

- 상태 인코딩: 보드 공개 카드, 각 플레이어의 볼/보너스/점수/보관/진화 경로를
  고정 길이 피처 벡터로.
- 작은 MLP(policy head: 행동 분포, value head: 승률 추정).
- 학습 루프: MCTS+현재 넷으로 자기대국 → (상태, 방문분포, 결과) 수집 →
  넷 학습 → 반복. 학습은 Python(PyTorch), 추론은 ONNX Runtime Web으로 브라우저 탑재.
- 사람 플레이 로그는 양이 적어 학습 데이터로는 부족 — 검증용으로만 사용.

### 성공 기준

- 2단계 MCTS(수작업 평가) 상대 유의한 승률 우위.

---

## 치트 모드 (단계와 병행 가능)

각 단계의 결과물을 그대로 노출하는 UI. 1단계 완료 시점부터 만들 수 있고,
단계가 올라갈수록 같은 UI의 정확도가 좋아진다.

- **승률 패널**: 매 턴 `simulateWinRates` 결과(플레이어별 %)를 표시.
  이미 계산 중이므로(`controller.ts`) 노출만 하면 된다.
- **최선 수 추천**: 합법 행동을 `actionValue`로 상위 K개(8~10) 필터 →
  각각 클론 상태에 적용 후 승률 시뮬레이션 → "이 수를 두면 내 승률 X%" 상위 3개 표시.
  기존 Worker 재활용, 사람 턴에만 계산.
- 토글(치트 ON/OFF)로 일반 플레이와 분리.

## 진행 상태

- [x] 1단계: 파라미터화 → 아레나 → CEM 튜닝 → 검증 (2026-07-30 인프라 완성, 튜닝 2회 실패)
- [x] **1단계 재시도 (2026-07-31 성공 — 가중치 교체)**: 적합도도 전체 예산으로 측정하자
  확증 매치 두 시드 모두 통과(46.0% / 44.6%, 기준선 25%).
- [x] 2단계: MCTS (2026-07-30 완료 — 아래 결과. 치트 모드 v2 훅 확보)
- [x] 치트 모드 (2026-07-30 완료): URL 에 `?cheat=1` 을 붙이면 사람 차례에
  MCTS 추천 수 상위 3개(행동·탐색 선호도·기대 가치)를 "나" 패널에 표시.
  Worker `hint` 요청으로 계산(비차단). 승률은 원래 기본 표시라 별도 처리 없음.
  의도적으로 URL 파라미터로만 켜짐(게임 재미 보호 — 단축키·저장 없음).
- [x] 분석 모드 (2026-07-30 완료): 헤더의 "분석" 토글 버튼. 켜면 사람 차례마다
  MCTS 통계를 미리 계산해두고, 행동 확정 시 최선 수 대비 가치 차이로 채점
  (🌟최선/👍좋음/🙂무난/😅아쉬움/💥실수, 최근 5건 로그). 계산 완료 전에 행동하면
  응답 도착 시 소급 채점. 치트 모드와 같은 Worker `hint` 요청을 공유.
- [ ] 3단계: 신경망 (선택)

## 1단계 실행 결과 (2026-07-30)

인프라(파라미터화·아레나·CEM 튜너)는 완성. 튜닝 2회 실행 결과 **가중치 미교체**:

| 라운드 | 적합도 | 1차 검증 | 확증 매치(다른 시드) | 판정 |
|---|---|---|---|---|
| 1 (seed1) | 승률, 24판/후보 | mean 31.7% ±5.9%p | 25.9%, 25.6% (총 880판 27.4% ±3.0%p) | 재현 실패 |
| 2 (seed2) | 순위점수, 48판/후보 | best 29.4% ±5.0%p | 24.6% ±3.9%p (총 800판 26.5% ±3.1%p) | 재현 실패 |

교훈(다음 시도 시 반영):
1. **1차 검증만 믿지 말 것.** 두 라운드 모두 첫 검증은 유의해 보였으나 새 시드 확증에서 무너졌다.
   반영 기준 = "서로 다른 시드 2회 이상에서 하한 > 기준선".
2. **예산 불일치 의심.** 적합도는 축소 탐색 예산(candidates 10/rollout 10)으로 측정했는데
   검증은 전체 예산 — 축소 예산에 과적합했을 가능성. 재시도 시 `--fitbudget=full` 권장(2배 느림).
3. **현 가중치는 이미 국소 최적 근방.** 같은 알고리즘에서 파라미터만으로 얻을 여지가 작다
   → 탐색 구조를 바꾸는 2단계(MCTS)의 기대 효과가 더 크다.
   → ⚠️ **이 결론은 2026-07-31 재시도에서 반박됐다.** 교훈 ②(예산 불일치)를 실제로 고치자
   파라미터 튜닝만으로 MCTS 와의 격차 대부분을 메웠다. 아래 "1단계 재시도" 절 참조.

장시간 재시도 예시(1~2시간): `npm run tune -- --gens=20 --pop=20 --games=96 --fitbudget=full --seed=3`

## 2단계 실행 결과 (2026-07-30)

`src/strategy/mcts.ts` — ISMCTS(덱 재셔플 determinization + UCT + 경량 정책 롤아웃 +
순위점수 벡터 역전파). 검증 매치(4인, MCTS 1 vs 기존 AI 3, 400회 반복, 기준선 25%):

| 시드 | 승률 | 순위점수(기준선 50) |
|---|---|---|
| 7 | 63.1% ±7.5%p (101/160) | 81.9 |
| 99991 (확증) | 66.9% ±7.3%p (107/160) | 82.7 |
| 991733 (재확증, 2026-07-31) | 59.1% ±5.4%p (189/320) | 78.5 |
| **합산** | **62.0% ±3.8%p (397/640)** | — |

기존 AI 대비 승률 2.5배 — 1단계에서 예상한 대로 "탐색 구조 교체"가 파라미터 튜닝보다
훨씬 큰 효과를 냈다.

> 이 표는 **당시의 휴리스틱 기준선** 상대 수치다. 2026-07-31 견제 가중치 교체(STRATEGY.md §4.2) 이후
> 같은 MCTS의 4인 승률은 **56.4% ±3.8%p (361/640) = 2.26배**로 내려갔다 — 기준선이 강해진 결과다.
> 현재 유효 수치는 아래 "인원수·견제 검증" 절을 따른다.

반영:

- **게임 AI 턴 = Worker MCTS**: `simulator/worker.ts`에 `aiturn` 요청 추가,
  `controller.aiMove()`가 비동기로 위임(메인 스레드 비차단, 턴당 ~0.5초).
  검증 매치와 동일한 400회 반복(`AI_MCTS_ITERATIONS`).
- 응답 불일치·이전 게임 응답은 방어적으로 무시하고 휴리스틱으로 대체.
- 검증 스크립트: `npx vite-node scripts/eval-mcts.ts -- --games=160 --iters=400`

치트 모드 v2 훅: `chooseMctsTurn()`이 루트 자식 통계(`stats`: 행동별 방문 수·평균 보상)를
반환하므로, 사람 턴에 같은 계산을 돌리면 "최선 수 랭킹 + 기대 가치"를 그대로 표시할 수 있다.

남은 개선 여지(선택): 롤아웃 정책 강화, 시간 예산 기반 반복(고정 400회 대신),
상대 비공개 보관 카드의 진짜 은닉 처리.

## 인원수·견제 검증 (2026-07-31)

지금까지의 모든 검증이 **4인 고정**(`eval-mcts.ts:NUM_PLAYERS = 4`)이었다는 점을 보완하기 위해
인원수(2·3·4인)를 파라미터화한 하네스 `scripts/eval-players.ts`(`npm run eval:players`)를 추가하고
세 가지 실험을 돌렸다. 원본: `tuning-results/players-*.json`.

### 결과 1 — MCTS 우위는 인원 무관 (`--mode=mcts`, 400회 반복)

**구 기본 가중치 기준선 상대** (시드 991733):

| 인원 | 승률 | 기준선 | 배수 |
|---|---|---|---|
| 2인 | 82.5% ±4.2%p (264/320) | 50% | 1.65배 |
| 3인 | 75.5% ±4.6%p (249/330) | 33.3% | 2.27배 |
| 4인 | 59.1% ±5.4%p (189/320) | 25% | 2.36배 |

**새 기본 가중치 기준선 상대** (§4.2 반영 후, 시드 4242 + 777001 합산):

| 인원 | 승률 | 기준선 | 배수 | 구 기준선 대비 |
|---|---|---|---|---|
| 2인 | 79.2% ±3.1%p (507/640) | 50% | 1.58배 | -3.3%p (시드별 74.1 / 84.4 — 편차 큼) |
| 3인 | 76.2% ±3.2%p (503/660) | 33.3% | 2.29배 | +0.7%p (변화 없음) |
| 4인 | **56.4% ±3.8%p (361/640)** | 25% | 2.26배 | **-5.6%p** |

**CEM 재튜닝까지 반영한 최종 기준선 상대** (현행 `WEIGHTS`, 시드 4242 + 777001 합산 — 문서 4부의 수치):

| 인원 | 승률 | 기준선 | 배수 |
|---|---|---|---|
| 2인 | 61.4% ±3.8%p (393/640) | 50% | 1.23배 |
| 3인 | 46.7% ±3.8%p (308/660) | 33.3% | 1.40배 |
| 4인 | 34.1% ±3.7%p (218/640) | 25% | 1.36배 |

MCTS 는 세 측정에서 동일한 알고리즘이다. 4인 62.0% → 56.4% → 34.1% 의 하락은 전부 **기준선이 강해진 결과**이며,
그 자체가 견제 개선(§4.2)과 재튜닝이 실제로 효과가 있었다는 독립 증거다. MCTS 는 여전히 모든 인원에서
기준선을 유의하게 상회한다(4인 하한 30.4% > 25%).

절대 승률은 인원이 늘수록 낮아진다(상대가 많아지면 누군가 운으로 앞설 여지도 커진다).
기준선 대비 배수는 구 기준선에서는 인원과 함께 커졌지만(1.65 → 2.27 → 2.36),
최종 기준선에서는 1.23 / 1.40 / 1.36 으로 평탄해졌다 — 기준선 개선 효과가 4인에서 가장 컸기 때문이다.

**§4.2 채택의 독립 증거**: MCTS는 양쪽에서 동일한 코드이고 바뀐 것은 나머지 좌석뿐인데 4인 승률이 -5.6%p 떨어졌다.
1 vs 3 매치의 비대칭 문제에 걸리지 않는 측정이므로, "새 기본값이 더 강한 상대"라는 결론을 뒷받침한다.
게다가 하락이 **4인에만** 나타나고 2·3인은 그대로인 점이 §4.1의 "이득은 4인에서 +5~9%p, 2·3인은 동등"과 일치한다.

### 결과 2 — 견제(blockValue) 어블레이션 → STRATEGY.md §4.1

조건당 480판 × 2시드 = 960판. 재현된 발견만 요약:

1. **`blockPts`(고점 카드 뺏기)는 6/6 측정에서 음수** — `V_card`가 이미 점수를 반영하므로 중복 계상이다.
2. **`blockBase`만 남긴 설정이 4인에서 +5~9%p** — 견제가 추가로 주는 정보는 "상대가 지금 지불 가능한가" 하나뿐.
3. **`blockNear`(선제 견제)는 재현 실패** → 기본값 0 유지.
4. `blockLeader` 단독(12점+ 저격)은 견제 없음과 구별되지 않는다 → 기존 "12점 이상일 때만" 정석은 과소.
5. 거울 실험(나머지 좌석 `block=0`): 2인 +5.4%p · 3인 +10.2%p · 4인 -1.0%p → 4인은 견제 이득이 제3자에게 흘러간다.

**반영 완료**: (2)를 근거로 `WEIGHTS` 를 `blockPts=blockEvo=blockNoble=blockLeader=0` 으로 교체하고
(STRATEGY.md §4.2 재검증), 이어서 CEM 재튜닝을 돌려 전체 벡터를 갱신했다("1단계 재시도" 절).
CEM 은 이 네 항목을 다시 올릴 기회가 있었는데도 0 근방에 두었다 → 결론의 독립 확인.

### 결과 3 — 인원별 게임 통계 (`--mode=stats`) → STRATEGY.md §8.2

2인 400판 · 3인 420판 · 4인 400판. 규칙에서 예측한 차이가 실측으로 확인됐다
(2·3인은 은행이 말라 볼을 3색 다 못 집는 턴이 21~22%, `take2` 사용률은 4인이 2.4배,
3단계 구매 시점의 해당 색 보너스는 2인 3.3 / 4인 2.4개, tie-break 발생률은 4인 11.8%).

### 부수 성과 — 버그 수정 1건

`mcts.ts:walk()` 의 "합법 행동 없음 → 강제 패스" 경로에 깊이 제한이 없어,
**모든 플레이어가 동시에 막히는 교착 상태에서 무한 재귀로 죽었다**(`RangeError: Maximum call stack size exceeded`).
2·3인전은 은행이 자주 말라 이 교착이 실제로 발생하므로 4인 전용 검증에서는 드러나지 않던 버그다.
깊이 제한 추가 + 회귀 테스트(`mcts.test.ts` "모든 플레이어가 막힌 교착 상태에서도 종료").

부수적으로 `blockValue`가 상대별 `legalMainActions` 전수 생성 대신 지불 가능성만 직접 계산하도록
바꿔(동작 동일, 턴 수 일치로 검증) 휴리스틱 자기대국이 4인 기준 약 2.7배 빨라졌다.

## 1단계 재시도 — 성공, 가중치 교체 (2026-07-31)

실행: `npm run tune -- --gens=20 --pop=20 --games=96 --fitbudget=full --seed=4 --jobs=10` (약 80분).
과거 실패 라운드와 달리 **적합도도 전체 탐색 예산으로 측정**(교훈 ②)하고 후보당 판수를 96으로 올렸다.
출발점은 견제 어블레이션을 반영한 가중치(§4.1 → `blockPts` 등 0).

적합도(순위점수, 기준선 50): gen1 56.9 → gen6 71.9 → gen11 74.3 → gen19 **75.0**.

확증 매치(4인, 후보 1 vs 기준 3, 480판, 전체 예산, 튜닝과 다른 시드):

| 후보 | 1차 검증(240판) | 확증 8181 | 확증 9292 | 합산(960판) | 판정 |
|---|---|---|---|---|---|
| best | 44.2% ±6.3%p | 44.0% ±4.4%p | 43.8% ±4.4%p | 43.9% | 기준 충족 |
| **mean** | 47.9% ±6.3%p | **46.0% ±4.4%p** | **44.6% ±4.4%p** | **45.3%** | **채택** |

두 시드 모두 하한(41.5% / 40.2%)이 기준선 25%를 크게 상회 → 채택 기준("서로 다른 시드 2회 이상에서
하한 > 기준선") 충족. `mean` 을 `weights.ts` 의 `WEIGHTS` 로 반영했다.

### CEM 이 견제 어블레이션 결론을 독립 확인했다

0 으로 내린 견제 항들은 σ=0.05 로 20세대 동안 자유롭게 다시 올라갈 수 있었는데도 전부 0 근방에 머물렀고,
대신 무차별 견제치와 견제 계수가 올라갔다 — §4.1 결론("점수·등급·진화는 중복 계상, 무차별만 남겨라")과 일치.

| | 재튜닝 전 | 재튜닝 후 |
|---|---|---|
| `blockBase` (무차별 견제치) | 1.8 | **2.251** ↑ |
| `block` (견제 계수) | 0.65 | **0.921** ↑ |
| `blockPts` / `blockEvo` / `blockNoble` / `blockLeader` | 0 | 0.005 / 0.031 / 0.030 / 0.058 (≈0 유지) |
| `blockNear` (선제 견제) | 0 | 0.033 (≈0 유지) |

그 외 큰 변화: `evalPts` 11 → 2.882, `costScale` 0.22 → 0.0297, `mixAfter` 0.58 → 0.0397,
`ballGoal` 0.55 → 0.909, `ptDiff` 8 → 9.951.

### 부작용 점검 — MCTS 전용 벡터 분리는 **불필요**로 결론

튜닝은 휴리스틱(`chooseStrongTurn`) 적합도로 돌렸지만 **MCTS 도 같은 벡터를 롤아웃·후보 pre-score 에 쓴다**.
그래서 "튜닝 벡터가 MCTS 에는 해로운가"를 분리 측정했다 — 상대를 튜닝된 휴리스틱으로 고정하고
MCTS 좌석의 가중치만 바꿔 320판 × 2시드 (`--mode=mcts --mctsw=prev`):

| 인원 | MCTS = 재튜닝 이후 | MCTS = 재튜닝 이전 | 차이 |
|---|---|---|---|
| 2인 | 61.4% (393/640) | 62.8% (402/640) | +1.4%p |
| 3인 | **46.7%** (308/660) | 41.8% (276/660) | **-4.9%p** |
| 4인 | 34.1% (218/640) | **38.3%** (245/640) | **+4.2%p** |

**처음에는 4인만 재고 `MCTS_WEIGHTS` 를 따로 두는 쪽으로 반영했다가, 2·3인을 재고 되돌렸다.**
4인은 이전 벡터가, 3인은 튜닝 벡터가 낫고 2인은 무차별 — 셀당 신뢰구간이 ±5.3%p 이므로
**차이를 검출하지 못했다**는 것이 정확한 결론이다. 단일 `WEIGHTS` 를 유지한다.

> 교훈(1단계 교훈 ①의 재확인): "두 시드에서 일관"은 **한 조건 안에서만** 성립하는 근거다.
> 4인 한 지점의 +4.2%p 를 신호로 받아들여 코드 복잡도(벡터 2개)를 늘렸지만,
> 조건을 인원수로 확장하자 노이즈로 드러났다. 여러 조건에서 확인하기 전에 구조를 바꾸지 말 것.

**후속 과제**: MCTS 자체를 적합도로 삼는 튜닝(자기대국 비용 20배 수준이라 미실행).
현재는 휴리스틱 적합도로 튜닝한 벡터를 MCTS 도 공유한다.
